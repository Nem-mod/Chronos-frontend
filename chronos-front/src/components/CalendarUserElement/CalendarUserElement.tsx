import { useEffect } from 'react';

interface Props {
    id: string;
}

//
// const fetchUserById = async () => {
//     return axios.get();
// };
export const CalendarUserElement = ({ id }: Props) => {
    useEffect(() => {

    }, []);
    return (
        <></>
    );
};
