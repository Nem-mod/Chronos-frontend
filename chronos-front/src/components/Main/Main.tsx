import { CalendarTest } from '../CalendarTest/CalendarTest.tsx';

export const Main = () => {
    return (
        <main className={'flex-grow pr-6 pl-6'}>
            <CalendarTest />
        </main>
    );
};
